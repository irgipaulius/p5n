/** Geo discovery grid for continuous / full / fetch-new passes. */

export type { Cell } from "./discovery-grid";
export { buildEuropeGrid, buildWorldGrid, worldGridCellAt, worldGridCellCount } from "./discovery-grid";
export { TYPE_CODE_LABELS as PLACE_TYPE_LABELS } from "../shared/place-types";
